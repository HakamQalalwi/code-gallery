Q2: Javascript Question For TAP CODING TEST

function findMissingNumber(nums) {
    const n = nums.length + 1;
    const expectedSum = (n * (n - 1)) / 2; // Sum of natural numbers from 0 to n
    let actualSum = 0;
    for (let i = 0; i < nums.length; i++) actualSum += nums[i];
    return expectedSum - actualSum;
}

console.log(findMissingNumber([3, 0, 1])); // Output: 2
console.log(findMissingNumber([9, 6, 4, 2, 3, 5, 7, 0, 1])); // Output: 8
console.log(findMissingNumber([0, 1]));
